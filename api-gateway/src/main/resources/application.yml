spring:
  application:
    name: api-gateway
  main:
    web-application-type: reactive
  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins:
              - http://localhost:4000
              - http://localhost:5173
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders:
              - "*"
            allowCredentials: true
      routes:
        - id: auth-service-route
          uri: http://auth-service:4051
          predicates:
            - Path=/user/**
          filters:
            - StripPrefix=1

        - id: post-service-route
          uri: http://post-service:4050
          predicates:
            - Path=/api/post/**
          filters:
            - StripPrefix=2
            - JwtValidation
          # Route for /api/story/** to stories-service
        - id: story-service-route
          uri: http://stories-service:4053
          predicates:
            - Path=/api/stories/**
          filters:
                - StripPrefix=2
                - JwtValidation

        - id: chat-service-route
          uri: http://chat-service:4052
          predicates:
            - Path=/api/chat/**
          filters:
            - StripPrefix=2
            - JwtValidation
            - RewritePath=/api/chat/(?<segment>.*), /$\{segment}
#            - name: JwtValidation
#              args:
#                - excludedPaths: /chat-ws/**

        - id: api-docs-users-route
          uri: http://auth-service:4051
          predicates:
            - Path=/api-docs/users
          filters:
            - RewritePath=/api-docs/users,/v3/api-docs

        - id: api-docs-post-route
          uri: http://post-service:4050
          predicates:
            - Path=/api-docs/post
          filters:
            - RewritePath=/api-docs/post,/v3/api-docs

#eureka:
#  client:
#    register-with-eureka: true
#    fetch-registry: true
#    service-url:
#      defaultZone: http://eureka-server:8761/eureka
#  instance:
#    prefer-ip-address: true

server:
  port: 4004

auth:
  service:
    url: http://auth-service:4051

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
